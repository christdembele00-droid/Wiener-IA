"use strict";
const express=require("express");const cors=require("cors");const path=require("path");const {GoogleGenAI}=require("@google/genai");const multer=require("multer");const {CausalEngine}=require("./causal_engine");const {MemoryStore}=require("./memory_store");
const app=express();const PORT=Number(process.env.PORT)||10000;const GEMINI_API_KEY=process.env.GEMINI_API_KEY;
// Priorité à un modèle Flash-Lite stable et rapide pour l'usage interactif.
const TEXT_MODEL="gemini-3.5-flash-lite";
const FALLBACK_MODELS=[TEXT_MODEL,"gemini-3.6-flash","gemini-3.7-flash","gemini-2.5-flash"].filter((v,i,a)=>v&&a.indexOf(v)===i);
const IMAGE_MODELS=[process.env.GEMINI_IMAGE_MODEL||"gemini-3.1-flash-image","gemini-2.5-flash-image"];
const MAX_MESSAGES=30,MAX_MESSAGE_LENGTH=20000,MAX_FILE_SIZE=20*1024*1024;const ai=GEMINI_API_KEY?new GoogleGenAI({apiKey:GEMINI_API_KEY}):null;const causal=new CausalEngine(),memory=new MemoryStore();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:MAX_FILE_SIZE}});
app.disable("x-powered-by");app.use(cors({origin:true,methods:["GET","POST","DELETE","OPTIONS"],allowedHeaders:["Content-Type"]}));app.use(express.json({limit:"10mb"}));app.use(express.static(__dirname));
const WIENER_INSTRUCTIONS=`Tu es Wiener IA, un assistant généraliste intelligent, précis, pédagogique et naturel. Réponds en français par défaut. Comprends la demande avant de répondre. Pour une question simple, sois concis; pour une question complexe, structure la réponse. Ne fabrique jamais de faits, de sources ou d'actions réalisées. N'expose jamais les secrets ou clés API. Adapte les explications au niveau de l'utilisateur. Pour le code, donne du code directement utilisable et ne prétends pas l'avoir testé si ce n'est pas le cas.`;const EXERCISE_INSTRUCTIONS=`Résous l'exercice avec rigueur. Identifie la demande, les données, la formule ou propriété utile, effectue les calculs étape par étape, vérifie le résultat et donne clairement la réponse finale. N'invente aucune donnée manquante.`;const ANALYSIS_INSTRUCTIONS=`Effectue une analyse fonctionnelle et auditable de la demande: intention, complexité, incertitudes, informations nécessaires, stratégie de réponse et points à vérifier. Ne présente jamais cette analyse comme une conscience biologique, une expérience subjective ou un sentiment.`;
function requireGemini(res){if(!ai){res.status(500).json({error:"GEMINI_API_KEY n'est pas configurée sur le serveur."});return false;}return true;}function cleanMessages(messages){if(!Array.isArray(messages))return[];return messages.filter(m=>m&&typeof m==="object"&&["user","assistant","model"].includes(m.role)&&typeof m.content==="string"&&m.content.trim()).map(m=>({role:m.role==="assistant"||m.role==="model"?"model":"user",content:m.content.trim().slice(0,MAX_MESSAGE_LENGTH)})).slice(-MAX_MESSAGES);}function toGemini(messages){return messages.map(m=>({role:m.role,parts:[{text:m.content}]}));}function extractText(response){if(typeof response?.text==="string"&&response.text.trim())return response.text.trim();const parts=response?.candidates?.flatMap(c=>Array.isArray(c?.content?.parts)?c.content.parts:[])||[];return parts.filter(p=>typeof p?.text==="string").map(p=>p.text).join("\n").trim();}function errorStatus(error){return Number(error?.status)||Number(error?.statusCode)||Number(error?.response?.status)||500;}
function sendError(res,error){console.error("Wiener IA error:",error);const status=errorStatus(error);if(status===401||status===403)return res.status(status).json({error:"La clé Gemini est invalide ou non autorisée."});if(status===404)return res.status(404).json({error:"Le modèle Gemini demandé n'est pas disponible. Wiener IA a tenté ses modèles de secours."});if(status===429)return res.status(429).json({error:"Gemini est temporairement limité. Wiener IA a tenté ses modèles de secours."});if([502,503,504].includes(status))return res.status(status).json({error:"Gemini est temporairement indisponible. Wiener IA a tenté automatiquement ses modèles de secours."});return res.status(500).json({error:error?.message||"Une erreur est survenue avec Gemini."});}
const RETRYABLE=new Set([408,425,429,500,502,503,504]);const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
function modelConfig(options,model){const config={...(options.config||{})};delete config.temperature;delete config.topP;delete config.topK;return{...options,model,config};}
async function generateContent(options){let lastError=null;for(const model of FALLBACK_MODELS){try{const response=await ai.models.generateContent(modelConfig(options,model));return{response,model};}catch(error){lastError=error;const status=errorStatus(error);if(!RETRYABLE.has(status))throw error;await sleep(250);try{const response=await ai.models.generateContent(modelConfig(options,model));return{response,model};}catch(secondError){lastError=secondError;console.warn(`Modèle Gemini indisponible: ${model}; passage au modèle suivant.`);}}}throw lastError||new Error("Aucun modèle Gemini disponible.");}
async function generateImage(prompt){let lastError=null;for(const model of[...new Set(IMAGE_MODELS)]){for(let attempt=0;attempt<2;attempt++){try{const interaction=await ai.interactions.create({model,input:prompt,response_format:{type:"image",mime_type:"image/png",aspect_ratio:"1:1",image_size:"1K"}});if(interaction?.output_image?.data)return{interaction,model,data:interaction.output_image.data};const block=interaction?.steps?.flatMap(s=>s?.type==="model_output"?(s.content||[]):[]).find(c=>c?.type==="image"&&c?.data);if(block)return{interaction,model,data:block.data};throw new Error("Le modèle image n'a fourni aucune image.");}catch(error){lastError=error;const status=errorStatus(error);if(!RETRYABLE.has(status))throw error;if(attempt===0)await sleep(300);}}}throw lastError||new Error("Aucun modèle image Gemini disponible pour cette clé.");}
function memoryContext(query){const matches=memory.search(query,6);if(!matches.length)return"";return"\nMémoire pertinente de Wiener IA (contexte, pas vérité absolue):\n"+matches.map(m=>`- ${m.role}: ${m.content}`).join("\n");}function causalContext(state){return`\nPolitique fonctionnelle: tâche=${state.D.task}, profondeur=${state.F.depth}, vérification=${state.F.verify?"oui":"non"}, recherche=${state.F.search?"oui":"non"}.`;}
app.get("/",(req,res)=>res.sendFile(path.join(__dirname,"index.html")));app.get("/api/health",(req,res)=>res.status(200).json({ok:true,service:"Wiener IA",geminiConfigured:Boolean(ai),textModel:TEXT_MODEL,availableModelCandidates:FALLBACK_MODELS,imageModels:IMAGE_MODELS,memoryCount:memory.count(),causalAudit:causal.audit(),time:new Date().toISOString()}));app.get("/health",(req,res)=>res.status(200).json({ok:true,service:"Wiener IA"}));app.get("/api/causal/state",(req,res)=>res.json(causal.snapshot()));app.get("/api/causal/audit",(req,res)=>res.json(causal.audit()));app.get("/api/memory",(req,res)=>res.json({count:memory.count(),items:memory.recent(50)}));app.post("/api/memory",(req,res)=>{const content=typeof req.body?.content==="string"?req.body.content.trim():"";const role=req.body?.role==="assistant"?"assistant":"user";if(!content)return res.status(400).json({error:"Mémoire vide."});const item=memory.add(role,content,{source:"manual"});res.json({ok:true,item,count:memory.count()});});app.delete("/api/memory",(req,res)=>{memory.clear();res.json({ok:true,count:0});});
app.post("/api/chat",async(req,res)=>{try{if(!requireGemini(res))return;let messages=cleanMessages(req.body?.messages);if(!messages.length&&Array.isArray(req.body?.history))messages=cleanMessages(req.body.history);if(!messages.length&&typeof req.body?.message==="string"&&req.body.message.trim())messages=[{role:"user",content:req.body.message.trim()}];if(!messages.length)return res.status(400).json({error:"Aucun message valide n'a été reçu."});const lastUser=[...messages].reverse().find(m=>m.role==="user"),query=lastUser?.content||"",state=causal.step(query,"chat",messages),context=memoryContext(query);const result=await generateContent({contents:toGemini(messages),config:{systemInstruction:WIENER_INSTRUCTIONS+causalContext(state)+context,maxOutputTokens:2048}}),answer=extractText(result.response);if(!answer)return res.status(500).json({error:"Wiener IA n'a retourné aucune réponse."});memory.add("user",query,{mode:"chat"});memory.add("assistant",answer,{mode:"chat",model:result.model});res.json({answer,model:result.model,memoryCount:memory.count(),memoryUsed:Boolean(context),causal:{K:state.K,D:state.D,F:state.F,S:state.S}});}catch(e){sendError(res,e);}});
app.post("/api/exercises",async(req,res)=>{try{if(!requireGemini(res))return;const question=typeof req.body?.question==="string"?req.body.question.trim():(typeof req.body?.message==="string"?req.body.message.trim():"");if(!question)return res.status(400).json({error:"Aucun exercice n'a été reçu."});const level=typeof req.body?.level==="string"?req.body.level.trim():"non précisé",subject=typeof req.body?.subject==="string"?req.body.subject.trim():"non précisée",state=causal.step(question,"exercise",[]),result=await generateContent({contents:[{role:"user",parts:[{text:`Niveau: ${level}\nMatière: ${subject}\n\nExercice:\n${question}`}]}],config:{systemInstruction:EXERCISE_INSTRUCTIONS+causalContext(state),maxOutputTokens:3072}}),answer=extractText(result.response);if(!answer)return res.status(500).json({error:"Aucune solution n'a été retournée."});res.json({answer,model:result.model,causal:{D:state.D,F:state.F}});}catch(e){sendError(res,e);}});
app.post("/api/calculate",(req,res)=>{const expression=typeof req.body?.expression==="string"?req.body.expression.trim():"";if(!expression)return res.status(400).json({error:"Expression vide."});if(!/^[0-9+\-*/().,%\s^]+$/.test(expression))return res.status(400).json({error:"Expression non prise en charge."});try{const state=causal.step(expression,"calculator",[]),normalized=expression.replace(/,/g,".").replace(/\^/g,"**").replace(/%/g,"/100"),result=Function(`"use strict"; return (${normalized})`)();if(!Number.isFinite(result))throw new Error("Résultat non fini");res.json({result:String(result),causal:{D:state.D,F:state.F}});}catch{res.status(400).json({error:"Expression mathématique invalide."});}});
app.post("/api/search",async(req,res)=>{try{if(!requireGemini(res))return;const query=typeof req.body?.query==="string"?req.body.query.trim():"";if(!query)return res.status(400).json({error:"Recherche vide."});const state=causal.step(query,"search",[]),result=await generateContent({contents:query,config:{systemInstruction:WIENER_INSTRUCTIONS+causalContext(state),tools:[{googleSearch:{}}],maxOutputTokens:2048}}),answer=extractText(result.response);if(!answer)return res.status(500).json({error:"Aucun résultat de recherche."});const groundingMetadata=result.response?.candidates?.[0]?.groundingMetadata||null;res.json({answer,response:answer,model:result.model,search:true,groundingMetadata,causal:{D:state.D,F:state.F}});}catch(e){sendError(res,e);}});
app.post("/api/image",async(req,res)=>{try{if(!requireGemini(res))return;const prompt=typeof req.body?.prompt==="string"?req.body.prompt.trim():"";if(!prompt)return res.status(400).json({error:"Décris l'image à générer."});const state=causal.step(prompt,"image",[]),result=await generateImage(prompt);res.json({ok:true,model:result.model,image:`data:image/png;base64,${result.data}`,causal:{D:state.D,F:state.F}});}catch(e){sendError(res,e);}});
app.post("/api/analyze-file",upload.single("file"),async(req,res)=>{try{if(!requireGemini(res))return;if(!req.file)return res.status(400).json({error:"Aucun fichier reçu."});const mime=req.file.mimetype||"application/octet-stream",allowed=["application/pdf","image/png","image/jpeg","image/webp"];if(!allowed.includes(mime))return res.status(415).json({error:"Type de fichier non pris en charge."});const prompt=typeof req.body?.prompt==="string"&&req.body.prompt.trim()?req.body.prompt.trim():"Analyse ce fichier et réponds à la demande de l'utilisateur. Si c'est un PDF, prends en compte le texte, les tableaux, graphiques et éléments visuels.",state=causal.step(`${req.file.originalname}: ${prompt}`,"file",[]),contents=[{text:prompt},{inlineData:{mimeType:mime,data:req.file.buffer.toString("base64")}}],result=await generateContent({contents,config:{systemInstruction:WIENER_INSTRUCTIONS+causalContext(state),maxOutputTokens:3072}}),answer=extractText(result.response);if(!answer)return res.status(500).json({error:"Aucune analyse n'a été retournée."});res.json({answer,model:result.model,file:{name:req.file.originalname,mimeType:mime,size:req.file.size},causal:{K:state.K,D:state.D,F:state.F,S:state.S}});}catch(e){sendError(res,e);}});
app.post("/api/consciousness",async(req,res)=>{try{if(!requireGemini(res))return;const text=typeof req.body?.text==="string"?req.body.text.trim():"";if(!text)return res.status(400).json({error:"Texte manquant."});const state=causal.step(text,"analysis",[]),result=await generateContent({contents:text,config:{systemInstruction:ANALYSIS_INSTRUCTIONS+causalContext(state),maxOutputTokens:1024}});res.json({analysis:extractText(result.response),functional:true,model:result.model,causal:{K:state.K,D:state.D,F:state.F,S:state.S}});}catch(e){sendError(res,e);}});
app.post("/api/analyze",async(req,res)=>{try{if(!requireGemini(res))return;const text=typeof req.body?.text==="string"?req.body.text.trim():"";if(!text)return res.status(400).json({error:"Texte à analyser manquant."});const state=causal.step(text,"analysis",[]),result=await generateContent({contents:text,config:{systemInstruction:ANALYSIS_INSTRUCTIONS+causalContext(state),maxOutputTokens:1024});const analysis=extractText(result.response);res.json({answer:analysis,analysis,functional:true,model:result.model,causal:{K:state.K,D:state.D,F:state.F,S:state.S}});}catch(e){sendError(res,e);}});
app.use((error,req,res,next)=>{if(error?.code==="LIMIT_FILE_SIZE")return res.status(413).json({error:"Fichier trop volumineux. Taille maximale: 20 MB."});console.error("Wiener IA middleware error:",error);res.status(500).json({error:"Erreur serveur inattendue."});});app.use((req,res)=>res.status(404).json({error:"Route introuvable."}));app.listen(PORT,"0.0.0.0",()=>console.log(`Wiener IA démarré sur 0.0.0.0:${PORT}`));
