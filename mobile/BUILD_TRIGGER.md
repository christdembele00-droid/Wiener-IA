# Wiener IA Android build

This file records the cloud-integrated Android build trigger for Wiener IA.

The mobile build uses the existing Render API for AI, Firestore persistence, and Cloudinary media storage. Server credentials remain outside the APK.
