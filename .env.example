# Gemini
GEMINI_API_KEY=
GEMINI_IMAGE_MODEL=gemini-3.1-flash-image

# Firebase Admin / Firestore (server-side only)
FIREBASE_PROJECT_ID=weiner-ia
FIREBASE_CLIENT_EMAIL=
# Keep the \n characters when storing the private key in Render or another host.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Cloudinary (server-side only)
# Option A: use Cloudinary's standard URL
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# Option B: use the three variables below instead
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Render supplies PORT automatically.
